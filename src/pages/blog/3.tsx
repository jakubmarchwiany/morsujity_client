import { Box, Typography } from "@mui/material";

function post3() {
    return (
        <Box mb={30}>
            <Typography variant="h2" mt={5} component="h1" textAlign="center">
                Motywacja
            </Typography>
            <Typography mt={10} mb={5} variant="body1" mx={10}></Typography>
        </Box>
    );
}

export default post3;
